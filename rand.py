#import random
# from random import *
# we can now use randint() freely without random. prefix
from random import randint

# import random, sys, os, math ## multiple imports
for i in range(10):
   print(randint(1,3))
